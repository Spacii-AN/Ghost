{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "My Discord Bot",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "deploy": "node dist/deploy-commands.js",
    "deploy-dev": "ts-node-dev --transpile-only deploy-commands.js"
  },
  "dependencies": {
    "discord.js": "^14.13.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}